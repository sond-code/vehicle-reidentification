# VeRi Explorer App 🚗

A web-based tool to explore the VeRi vehicle re-identification dataset and extract CNN features using models like ResNet and MobileNet.

## Features
- Dataset dashboard
- Image explorer
- Feature extraction (ResNet, MobileNet)
- Job tracking

## Run locally

```bash
pip install -r requirements.txt
python app.py
